dist: trusty
language: node_js
node_js:
- "node"
- "12"
- "10"
env:
  - CC=clang CXX=clang++ npm_config_clang=1
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
      - libmagick++-dev
